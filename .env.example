# Example environment variables for Cognify

# Supabase project URL (find in your Supabase dashboard under Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase anon public key (find in your Supabase dashboard under Project Settings → API)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# The base URL of your site (use http://localhost:3000 for local development)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# (Optional) Add any other environment variables your project requires below
# e.g. AI_API_KEY=your-ai-api-key

# !!! Recommended to use Vercel (vercel env pull .env.local) to pull the env variables!